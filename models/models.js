// Define schema structure database. A way to normalize data as javascript and treat them as objects..SQL or no SQL database

// ORM w mongoose to organize queries to normalize javascript and treat them as objects
